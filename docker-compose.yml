version: "3.9"
services:
#  web:
#    build: .
#    ports:
#      - "5000:5000"
  database:
    image: 'postgres:latest'
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: sloth
      POSTGRES_PASSWORD: sloth
      POSTGRES_DB: sloth